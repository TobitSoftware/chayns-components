export interface ITooltipItem {
    headline?: string;
    text: string;
}

export enum TooltipAlignment {
    TopLeft,
    TopCenter,
    TopRight,
    BottomLeft,
    BottomCenter,
    BottomRight,
}
