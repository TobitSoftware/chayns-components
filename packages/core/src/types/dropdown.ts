export interface DropdownCoordinates {
    x: number;
    y: number;
}

export enum DropdownAlignment {
    BOTTOM,
    TOP,
    BOTTOM_LEFT,
    BOTTOM_RIGHT,
    TOP_LEFT,
    TOP_RIGHT,
    LEFT,
    RIGHT,
}
