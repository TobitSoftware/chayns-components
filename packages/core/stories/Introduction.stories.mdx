
import { Meta } from '@storybook/addon-docs';

<Meta title="Introduction" />

<link
    href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700;900&display=swap"
    rel="stylesheet"
/>

<div style={{ textAlign: 'center' }}>
    <img
        src="https://chayns.space/77896-21884/chayns_components.png"
        width="600px"
        alt="chayns-components"
    />
    <p style={{ fontSize: '85%', marginTop: 0, opacity: 0.85 }}>
        A modern, modular React component library for building powerful chayns® applications.
    </p>
</div>

---

## Overview

Welcome to the Storybook documentation for the **chayns-components**.
This library has been completely reworked using modern React patterns.
Use the navigation on the left to explore the available components.
Core components are listed under the `Core` section, while additional packages are grouped separately.

> <sup>1</sup> In the latest version, all components have been rewritten as functional components with React Hooks. Animations now use **Framer Motion**, and the component props have been simplified and improved for better clarity and usability.

---

## Installation

This library is modular. Core and additional packages must be installed separately using either `npm` or `yarn`.

<details>
    <summary>Install using NPM</summary>

    ```bash
    # Core components
    npm install @chayns-components/core

    # Additional components
    npm install @chayns-components/code-highlighter
    npm install @chayns-components/color-picker
    npm install @chayns-components/date
    npm install @chayns-components/devalue-slider
    npm install @chayns-components/emoji-input
    npm install @chayns-components/format
    npm install @chayns-components/gallery
    npm install @chayns-components/maps
    npm install @chayns-components/person-finder
    npm install @chayns-components/swipeable-wrapper
    npm install @chayns-components/textstring
    npm install @chayns-components/translation
    npm install @chayns-components/typewriter
    ```

</details>

<details>
    <summary>Install using Yarn</summary>

    ```bash
    # Core components
    yarn add @chayns-components/core

    # Additional components
    yarn add @chayns-components/code-highlighter
    yarn add @chayns-components/color-picker
    yarn add @chayns-components/date
    yarn add @chayns-components/devalue-slider
    yarn add @chayns-components/emoji-input
    yarn add @chayns-components/format
    yarn add @chayns-components/gallery
    yarn add @chayns-components/maps
    yarn add @chayns-components/person-finder
    yarn add @chayns-components/swipeable-wrapper
    yarn add @chayns-components/textstring
    yarn add @chayns-components/translation
    yarn add @chayns-components/typewriter
    ```

</details>

> **Note:** Styles are bundled within each package and are applied automatically.

---

## Usage

First, make sure to install the core package and any additional components you need. Then, wrap your application using the `ChaynsProvider` and apply the color scheme via `ColorSchemeProvider`:

```tsx
import React from 'react';
import { ChaynsProvider } from 'chayns-api';

const AppWrapper = () => (
    <ChaynsProvider>
        <App />
    </ChaynsProvider>
);

export default AppWrapper;
```

```tsx
import React from 'react';
import { ColorSchemeProvider } from '@chayns-components/core';
import { getSite } from 'chayns-api';

const App = () => {
    const { color, colorMode } = getSite();

    return (
        <ColorSchemeProvider color={color} colorMode={colorMode}>
            <YourComponent />
        </ColorSchemeProvider>
    );
};

export default App;
```

---

## Contribution

Make sure you're using **Node.js v16** or higher. To get started:

```bash
# NPM
npm install

# Yarn
yarn install
```

Build the packages locally:

```bash
# NPM
npm run lerna:build

# Yarn
yarn run lerna:build
```

Start the local Storybook instance to begin development:

```bash
# NPM
npm run storybook

# Yarn
yarn run storybook
```

> **Note:** You can access the local Storybook at [http://localhost:6006](http://localhost:6006)
