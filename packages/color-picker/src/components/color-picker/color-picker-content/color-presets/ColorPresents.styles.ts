import styled from 'styled-components';

export const StyledColorPresets = styled.div`
    display: flex;
    flex-wrap: wrap;
    gap: 4px;
    width: 300px; // ToDo: Find better solution to fix wrapping of dots at correct width
`;
