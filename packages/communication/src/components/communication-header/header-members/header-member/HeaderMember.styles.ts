import styled from 'styled-components';

export const StyledHeaderMember = styled.div`
    cursor: pointer;
    width: fit-content;

    border-bottom: 1px dashed;

    span:first-of-type {
        padding: 0;
    }

    &:hover {
        opacity: 0.75;
    }
`;
